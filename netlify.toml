[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: let React Router (BrowserRouter) handle client-side routes
# so deep links / hard refreshes don't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
