project_id = "pmiglnfoalieflbzxtfa"

[functions.receive-from-make]
verify_jwt = false

[functions.send-to-make]
verify_jwt = false

# Requires a logged-in user; the function additionally re-verifies the caller
# and only then uses the service-role key to manage users.
[functions.admin-create-user]
verify_jwt = true
